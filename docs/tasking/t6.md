Update development methodology. 

For any work, make a work item in docs/planning with the syntax [epic]/[feature]/[userStory]. Each epic should have a .md file describing the general purpose of the epic. Each feature should have a .md file describing the general purpose of the feature. Each userStory should have a .md file describing the specific work to be done. Use the same syntax for all work items. Each userStory should be linked to the feature and epic it belongs to. Use a central index file to link to all epics, features, and userStories in the system. This will help maintain organization and clarity in the development process. Each userStory should have a summary, acceptance citeria, and any context. 

Code development should be done on a single work item at a time. When the work for a userStory is complete, commit it with a descriptive message that includes the userStory identifier. This will help track changes and ensure that each piece of work is clearly documented. Each userStory should be testable and have a clear definition of done. Each userStory should have unit tests. Commits should only be made when the userStory is complete and passes all unit tests.

Each feature should have a clear set of acceptance criteria that must be met before it can be considered complete. This will help ensure that the feature meets the requirements and is ready for use. A feature should be a set of work that can result in a successful build. Testing at the feature level should include integration tests that cover the feature's functionality and interactions with other features.

Use robust logging and error handling throughout the codebase. This will help identify issues quickly and ensure that the application runs smoothly. Each feature should have a clear set of unit tests that cover its functionality. These tests should be run as part of the build process to ensure that the feature works as expected. Include verbose logging for testing purposes and succinct logging for production use. This will help with debugging and ensure that the application runs efficiently in production.