Add functionality to track the preferences and progress of multiple users. A single listing may be utilized by multiple users with different preferences and progress states. The data about the user's opinion of a listing should be stored separately from the listing itself.

In planning docs, make a separate file for each data concept. User preferences, users, and listings should each have their own file. Apply this pattern to all data concepts in the system. Use a central data model file to index and link to these files.

